{
	"name": "fintual-api",
	"module": "index.ts",
	"type": "commonjs",
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@playwright/test": "^1.52.0",
		"@types/bun": "latest",
		"@types/node": "^22.14.1"
	},
	"peerDependencies": {
		"typescript": "^5.8.3"
	},
	"dependencies": {
		"@actual-app/api": "^25.9.0",
		"@types/random-useragent": "^0.3.3",
		"cron": "^4.3.0",
		"dotenv": "^16.5.0",
		"playwright": "^1.52.0",
		"random-useragent": "^0.5.0",
		"valibot": "^1.0.0"
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && node dist/once.js",
    "delete": "tsc -p tsconfig.json && node dist/delete-transactions.js",
    "play": "tsc -p tsconfig.json && node dist/play.js",
		"once": "node dist/once.js",
    "new-performance": "tsc -p tsconfig.json && node dist/fintual/new-performance.js",
		"scheduler": "node dist/scheduler.js"
	}
}
